Kubernetes manifests go here (Step 6): receiver-deployment.yaml, worker-deployment.yaml,
receiver-service.yaml, secrets (via kubectl create secret, not committed), postgres
(or point at RDS once on AWS).
